@model IEnumerable<BusSchedule.Data.Driver>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.DrverName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DrverPhone)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DrverAddress)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DrverNID)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.DrverName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DrverPhone)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DrverAddress)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DrverNID)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.DriverID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.DriverID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.DriverID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
